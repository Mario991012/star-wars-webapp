import { SpeedFormatterPipe } from './speed-formatter.pipe';

describe('SpeedFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new SpeedFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
